# driver_controller_ws


## compile

at root of the repo : 

catkin build


## run 

in a terminal run (if no master node already launched): 

roscore

in another one :

rosun driver_controller <executable name>
